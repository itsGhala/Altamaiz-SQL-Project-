# Altamaiz-SQL-Project-
A simple SQL project script where I created a database with 3 tables (Student, Teacher, and Course) inserted sample data, and used basic SQL commands such as : CREATE, INSERT, SELECT, UPDATE, ALTER, ORDER BY.
